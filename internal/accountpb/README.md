Vendored copy of `account/proto/openpak/account/v1` (generated stubs and their `.proto`
sources) so a clean checkout and the release image build without a `replace` to the sibling
core repository. Regenerate by copying from `../../../account/proto/openpak/account/v1`.
